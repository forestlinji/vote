

server:
  port: 11451
spring:
  jackson:
    serialization:
      write-dates-as-timestamps: true
  http:
    encoding:
      force: true
      charset: utf-8
      enabled: true
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vote?useSSL=false&serverTimezone=CTT&useUnicode=true&characterEncoding=UTF-8

